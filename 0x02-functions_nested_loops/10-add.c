#include "main.h"
/**
 * add - takes two numbers from input
 * @a: first aparamet
 * @b: second parameter
 * Description: add two numbers
 * Return: Always (0).
 */
int add(int a, int b)
{
	return (a + b);
}
